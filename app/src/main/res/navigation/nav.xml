<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav"
    app:startDestination="@id/fragmentLogin">
    <fragment
        android:id="@+id/fragmentLogin"
        android:name="com.marcr.pescapp.login.FragmentLogin"
        android:label="FragmentLogin" >
        <action
            android:id="@+id/action_fragmentLogin_to_fragmentRegistre"
            app:destination="@id/fragmentRegistre" />
    </fragment>
    <fragment
        android:id="@+id/fragmentRegistre"
        android:name="com.marcr.pescapp.registre.FragmentRegistre"
        android:label="FragmentRegistre" >
        <action
            android:id="@+id/action_fragmentRegistre_to_fragmentLogin"
            app:destination="@id/fragmentLogin" />
    </fragment>
</navigation>